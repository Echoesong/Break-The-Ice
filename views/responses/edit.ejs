<%- include("../partials/header", {title}) %>

    <div class="container mb-3">
        <div class="row align-items-center">
            <div class="col-8">
                <h3><%= icebreaker.content%></h3> 
            </div>
            <div class="col-4">
                <h3>Topic: <%= icebreaker.topic%></h3>
            </div>
        </div>
    </div>

    <div class="container">
        <form action="/responses/<%=response._id%>?_method=PUT" method="POST">

            <div class="form-floating mb-3">
                <input type="text" name="content" class="form-control" id="responseInput" placeholder="Edit your response" value="<%=response.content%>" required>
                <label for="responseInput">Edit your response</label>
            </div>

            <div class="form-floating mb-3">
                <input type="text" name="author" class="form-control" id="authorInput" placeholder="Edit your name" value="<%=response.author%>" required>
                <label for="authorInput">Edit your name</label>
            </div>

            <div class="d-grid gap-2 col-4 mx-auto">
                <button class="btn btn-primary btn-md" type='submit'>Submit</button>
            </div>
        </form>
    </div>

<%- include("../partials/footer") %>